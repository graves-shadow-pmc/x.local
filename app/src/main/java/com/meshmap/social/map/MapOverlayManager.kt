package com.meshmap.social.map

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

@Composable
fun MeshZoneOverlay(
    zoneName: String,
    modifier: Modifier = Modifier
) {
    // For production: Use Google Maps Compose or Mapbox overlays
    // to visualize mesh borders and user's location.
}